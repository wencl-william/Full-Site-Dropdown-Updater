// function policyRename() //Coment out when not in use to avoide accidental trigger
// {
//    const filePath = ['District','Policies-Live', '*District Policy Manual']; //The folder path that houses to the Live folder.
//    var folder = DriveApp.getFoldersByName(filePath[0]).next(); 
//    for(var i = 1; i < filePath.length; i++)
//    {
//        folder = folder.getFoldersByName(filePath[i]).next(); 
//    }
//    renamePolicies(folder);
// } 



/*Out of Order Function throws Error 
function owner()
{
    const filePath = ['District','Policies-Live']; //The folder path that houses to the Live folder.
    var folder = DriveApp.getFoldersByName(filePath[0]).next(); 
    for(var i = 1; i < filePath.length; i++)
    {
        folder = folder.getFoldersByName(filePath[i]).next(); 
    }
    transferOwner(folder);
}*/

